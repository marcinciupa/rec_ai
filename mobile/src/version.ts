// Numer builda (jedno źródło prawdy dla wyświetlania w apce). Trzymać w synchronie z app.json:
//   version === APP_VERSION,  versionCode === round(APP_VERSION * 1000)  (0.951 → 951, 0.952 → 952, …).
// Konwencja 0.XXX; każdy kolejny build = +0.001.
export const APP_VERSION = '0.962';
